STRAPI_API_URL=https://your-strapi-instance.com
STRAPI_API_TOKEN=your_strapi_api_token_here
# ===================================
# DEPLOYMENT NOTES
# ===================================
# 
# For production deployment, you only need to set the Strapi
# variables if you're using the migration endpoint:
#
# npx supabase secrets set STRAPI_API_URL=value STRAPI_API_TOKEN=value
#
# Or from a file:
# npx supabase secrets set --env-file .env.production
#
# Variables NOT needed in production:
# - SUPABASE_URL (automatically provided)
# - SUPABASE_ANON_KEY (automatically provided)
# - SUPABASE_SERVICE_ROLE_KEY (automatically provided)
# - TEST_USER_PASSWORD (only for local testing)
# - TEST_USER_EMAIL_PREFIX (only for local testing)
# - EXTERNAL_KEY (appears to be unused)
# - SUPER_PASSWORD (no longer used after auth refactor)
# - level95 (only for testing)
# - SUPABASE_ACCESS_TOKEN (only for CLI deployment)
# - SUPABASE_DB_PASSWORD (only for CLI deployment)
# - SUPABASE_PROJECT_ID (only for CLI deployment)